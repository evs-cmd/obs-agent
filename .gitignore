# Secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Virtualenvs
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data caches
data/chroma/
*.log

# Eval reports
evals/last_report.json
evals/*.report.json

# Build
build/
dist/

# Observability sinks (src/obs)
events.jsonl
replays/

# DuckDB local analytics (scripts/dashboard.py)
obs.duckdb
*.duckdb.wal
*.duckdb.tmp/

# Optional external MCP backend — operators drop this in to enable OBS_BACKEND=otel.
# Without it, the selector falls back to the mock backend with a warning.
src/mcp/tools/_backends/otel.py
tests/test_otel_backend.py

# Refactor scratch — incident agent package split kept on disk for reference
# but not part of the shipped tree. __init__.py files were removed so Python
# imports src/agents/incident.py (the working single-file module) instead.
src/agents/incident/


.cache/
.chainlit/
.files/
chainlit_app.py
chainlit.md
.deepeval/
.dockerignore
.langgraph_api/
prod/
